server:
  port: 8083
  servlet:
    session:
      timeout: 60
spring:
  session:
    store-type: redis
  redis:
    port: 6379
    host: 127.0.0.1
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
    database: 0
    password:
